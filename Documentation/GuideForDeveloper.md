# Инструкция для разработчика (Guide for Developer)

Этот документ предназначен для коллег, которым необходимо запустить проект или подготовить его к сдаче заказчику.

## 🚀 Быстрый запуск

Для запуска всех сервисов (БД, Бэкенд, Бот, Фронтенд) используйте команду:

```bash
make up
```

## 🛠 Подготовка к сдаче (Очистка данных)

Перед сдачей проекта заказчику необходимо удалить данные, накопленные в ходе тестирования (ученики, их результаты, история состояний бота).

### Вариант 1: Полная очистка всей базы
Если нужно удалить **абсолютно всё**, включая загруженные вопросы:

```bash
docker-compose exec web python manage.py flush
```
*После этой команды нужно будет заново создать суперпользователя и загрузить вопросы через Excel.*

### Вариант 2: Выборочная очистка (Рекомендуется)
Если нужно удалить **только бета-тестеров и их результаты**, сохранив базу вопросов:

1. Зайдите в терминал и выполните shell:
   ```bash
   docker-compose exec web python manage.py shell
   ```
2. Вставьте этот код для удаления данных тестирования:
   ```python
   from apps.models import BotUser, TestAttempt, AttemptDetail, BotState
   AttemptDetail.objects.all().delete()
   TestAttempt.objects.all().delete()
   BotState.objects.all().delete()
   BotUser.objects.all().delete()
   exit()
   ```

## 🔑 Управление доступом

1. **Создание нового администратора:**
   ```bash
   make superuser
   ```
2. **Админ-панель:** [http://localhost:8000/admin/](http://localhost:8000/admin/)
3. **Фронтенд-дашборд:** [http://localhost:5173/](http://localhost:5173/)

## 📁 Добавление контента
После очистки базы загрузите финальный список вопросов через интерфейс "Questions -> Import Excel" в админке или через фронтенд на вкладке "Вопросы".
