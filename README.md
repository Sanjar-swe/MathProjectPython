# UStudy Test Bot & Admin Panel

Проект представляет собой систему тестирования через Telegram-бота с веб-панелью администратора для управления вопросами и просмотра статистики.

![Dashboard Screenshot](https://via.placeholder.com/800x400?text=UStudy+Admin+Dashboard)

## 🚀 Функционал

### Telegram Бот
- 👤 **Регистрация:** Привязка Telegram ID к имени и фамилии.
- 🚫 **Анти-чит:** Один аккаунт Telegram = Один ученик.
- 📝 **Тестирование:** 10 случайных вопросов из базы.
- ⏭️ **Пропуск вопросов:** Возможность пропустить сложный вопрос.
- 📊 **Результаты:** Мгновенный подсчет баллов и список ошибок.
- 💾 **Надежность:** Сохранение прогресса (FSM) в базе данных (защита от отключения света).

### Админ-панель (Frontend)
- 📈 **Дашборд:** Статистика по ученикам, попыткам и среднему баллу.
- 📤 **Импорт:** Загрузка вопросов из Excel (Drag & Drop).
- 📋 **Список учеников:** Просмотр всех зарегистрированных пользователей с их ID.
- 📱 **UI:** Современный интерфейс на React + TailwindCSS.

---

## 🛠 Технический стек

- **Backend:** Python 3.12, Django 6.0, Django REST Framework.
- **Bot:** Aiogram 3.x (Asynchronous).
- **Frontend:** React 19, Vite, TailwindCSS v4.
- **Database:** PostgreSQL.
- **Dependency Manager:** uv (Python), npm (JS).

---

## ⚙️ Установка и Запуск

### Предварительные требования
- Python 3.12+
- Node.js 18+
- PostgreSQL
- Установленный `uv` (рекомендуется) или `pip`

### 1. Настройка Бэкенда

1. Создайте файл `.env` в корне проекта на основе примера:
   ```env
   DEBUG=on
   SECRET_KEY=your_secret_key
   DATABASE_URL=postgres://user:password@localhost:5432/db_name
   BOT_TOKEN=your_telegram_bot_token
   ```

2. Установите зависимости:
   ```bash
   uv sync
   # ИЛИ, если используете pip:
   # pip install -r requirements.txt
   ```

3. Примените миграции:
   ```bash
   uv run python manage.py migrate
   ```

4. Запустите сервер:
   ```bash
   uv run python manage.py runserver 0.0.0.0:8000
   ```

### 2. Запуск Бота

В отдельном терминале запустите бота:
```bash
uv run python -m apps.bot.main
```

### 3. Запуск Фронтенда

1. Перейдите в папку фронтенда:
   ```bash
   cd frontend
   ```

2. Установите зависимости:
   ```bash
   npm install
   ```

3. Запустите сервер разработки:
   ```bash
   npm run dev
   ```

Откройте [http://localhost:5173](http://localhost:5173) в браузере.

---

## 📁 Структура Excel файла для импорта

Файл должен быть формата `.xlsx` и содержать следующие колонки (без заголовка, начиная со 2-й строки, или с заголовком - скрипт адаптирован):

| A | B | C | D | E | F |
|---|---|---|---|---|---|
| Текст вопроса | Вариант A | Вариант B | Вариант C | Вариант D | Правильный ответ (a/b/c/d) |

---

## 🔧 Полезные команды

- **Создать суперпользователя:** `uv run python manage.py createsuperuser`
- **Очистить базу (для тестов):** `uv run python manage.py flush`
- **Генерация тестового Excel:** `uv run python create_test_excel.py`
